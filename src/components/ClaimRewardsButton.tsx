const ClaimRewardsButton: React.FC = () => {
    const handleClaimRewards = () => {
        // Add the logic for claiming rewards here.
    };

    return (
        <button className="..." onClick={handleClaimRewards}>
            Claim Rewards
        </button>
    );
};

export default ClaimRewardsButton;
